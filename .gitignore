.venv/
.venv311/
__pycache__/
*.pyc
.env
.idea/
#data/
data/fetch_preview/
data/ocr_preview/
# Regenerated by review_dashboard.py after every /trigger run — the
# generator script is tracked, the output (which changes on every run and
# can run 1-2MB) is not.
review_dashboard.html
# Same pattern for presentation.py — published to Artifacts, republished
# from this file each time; the file itself is a build intermediate.
presentation_fragment.html
